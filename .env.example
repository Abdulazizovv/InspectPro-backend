###############################################
# Base
###############################################
DEBUG=true                     # Set false in production
DJANGO_SECRET_KEY=change_me    # Required; used by Django
ALLOWED_HOSTS=localhost,127.0.0.1  # Comma-separated; add your domain(s)
DJANGO_MAX_BODY_BYTES=2000000  # Max request body size for Django
COMPOSE_PROJECT_NAME=inspectpro-backend

###############################################
# Database (choose one)
###############################################
# Option A: Use SQLite (easy local dev)
# USE_SQLITE=true

# Option B: Use DATABASE_URL (Heroku/Render/etc.)
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# Option C: Explicit Postgres params (Docker Compose defaults)
POSTGRES_DB=app_db
POSTGRES_USER=app_user
POSTGRES_PASSWORD=app_password
DB_HOST=inspectpro_db
DB_PORT=5432

###############################################
# Telegram Bot
###############################################
BOT_TOKEN=123456:ABC-DEF_fake_token
ADMINS=123456789              # Comma-separated list if multiple
TELEGRAM_WEBHOOK_DOMAIN=https://your-domain.example
TELEGRAM_WEBHOOK_PATH=/api/telegram/webhook
TELEGRAM_WEBHOOK_SECRET=super_secret_header_token
TELEGRAM_WEBHOOK_MAX_BODY=2000000

###############################################
# Redis (optional; reserved for future use)
###############################################
REDIS_URL=redis://inspectpro_redis:6379/0
REDIS_HOST=inspectpro_redis
REDIS_PORT=6379
REDIS_DB=0

###############################################
# Misc
###############################################
LOG_LEVEL=INFO
LOG_BACKUP_COUNT_DAYS=14
LOG_ROTATE_UTC=true

###############################################
# Celery
###############################################
CELERY_BROKER_URL=redis://inspectpro_redis:6379/1
CELERY_RESULT_BACKEND=redis://inspectpro_redis:6379/2
CELERY_ACCEPT_CONTENT=json
CELERY_TASK_SERIALIZER=json
CELERY_RESULT_SERIALIZER=json
CELERY_TIMEZONE=Asia/Tashkent

###############################################
# Flower
###############################################
FLOWER_BASIC_AUTH=admin:admin

###############################################
# CORS (Frontend origins)
###############################################
# Frontend portiga mos bo'lishi kerak (frontend docker-compose FRONTEND_PORT)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

###############################################
# Backend nginx port
###############################################
BACKEND_PORT=8080
